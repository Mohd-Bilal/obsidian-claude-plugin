# Path to your Obsidian vault (overrides auto-discovery)
OBSIDIAN_VAULT_PATH=/path/to/your/vault

# Auto-commit interval (e.g. "15m", "1h", "300s"). Leave unset to disable.
GIT_AUTOCOMMIT_INTERVAL=15m

# Folder inside the vault that contains templates
OBSIDIAN_TEMPLATES_FOLDER=Templates
